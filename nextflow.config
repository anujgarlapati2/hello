process.executor = 'google-batch'

executor {
    name = 'google-batch'
}

google.project = 'tower-nf-444010'   

workDir = 'gs://seqera-sa-bucket/work'  // Replace with your GCS bucket for work directory

process {
    container = 'quay.io/nextflow/bash'  // Specify the container image
}
